[ai-inbound]
exten => 2001,1,Answer()           ; ✅ Answers immediately
same => n,Playback(beep)        ; 🔥 ADD THIS
same => n,Playback(silence/1)   ; 🔥 THIS IS THE FIX
same => n,Stasis(ai_agent)       ; ✅ Enters Stasis app "ai-agent" 
same => n,Hangup()               ; ✅ Clean hangup

[cleanup]
exten => s,1,Hangup(16)  ; NORMAL_CLEARING
 same => n,Return()
